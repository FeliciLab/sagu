<?php

class FrmTipoDeDocenteSearch extends SSearchForm
{
    protected $campoBuscaGenerica = true;
    
    public function __construct()
    {
        parent::__construct(null, new AcpTipoDeDocente(), array('tipodedocenteid'));
    }
    
    public function defineFields()
    {
        $MIOLO = MIOLO::getInstance();
        $module = $MIOLO->getCurrentModule();
        
        parent::defineFields($fields, $module, 'GrdTipoDeDocenteSearch', $validators);
    }
}